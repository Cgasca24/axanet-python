name: 02 - Modificación de cliente existente
on:
  push:
    paths:
      - "data/clients/**"

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Notificar a equipo
        run: |
          echo "Modificación de cliente detectada"
          echo "Notificar a: DevA, DevB, IT1, IT2, Atención1, Atención2"
